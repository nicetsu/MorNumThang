# แผนอัปเกรดการคำนวณคะแนนความเสี่ยง (risk score)

สถานะ: **บันทึกไว้ (ยังไม่ implement)** · ที่มา: research รอบเบา (NEWS2 + red-flag)

## ปัญหาที่จะแก้
ปัจจุบัน AI ให้ `severity` 0–10 ต่ออาการ แล้ว band 8–10 = แดง → อาการธรรมดา (เช่น "ปวดท้อง")
เด้งแดงง่ายเกินไป (over-triage, alarm fatigue). ต้องการให้การขึ้นแดงอิงหลักฐานที่วัดได้ +
กติกาที่ตรวจสอบได้ ไม่ใช่ตัวเลขทึบจาก LLM (คง AGENTS.md rule 2 — deterministic เท่านั้นที่ตัดสินความปลอดภัย).

## แนวทาง: แยกเป็น 2 track อย่าให้ LLM คิดคะแนน
1. **Vital-sign risk = NEWS2** — นับแต้มจากค่าที่วัดได้ (auditable 100%)
2. **Symptom red-flag = rule table** — อาการธรรมดาไม่แดง เว้นแต่มี modifier อันตราย

LLM ทำแค่จัดหมวด/สกัดคำ (เช่น เจอ "อาเจียนเป็นเลือด" ไหม) แล้วส่งให้ code ตัดสิน.

## NEWS2 — ตารางนับแต้ม (ค่ามาตรฐาน RCP)
แอปเก็บ/track อยู่แล้ว: **ความดันตัวบน, ชีพจร, ไข้ (care task "วัดไข้"), SpO2 ("วัดออกซิเจนปลายนิ้ว")**
ที่ยังไม่มี: อัตราหายใจ, ระดับรู้สึกตัว (อาจเพิ่มเป็น care task/ช่องกรอกภายหลัง).

| พารามิเตอร์ | 3 | 2 | 1 | 0 |
|---|---|---|---|---|
| หายใจ/นาที | ≤8, ≥25 | 21–24 | 9–11 | 12–20 |
| SpO2 % | ≤91 | 92–93 | 94–95 | ≥96 |
| ให้ออกซิเจนเสริม | — | ใช่ | — | ไม่ |
| อุณหภูมิ °C | ≤35.0 | ≥39.1 | 35.1–36.0 / 38.1–39.0 | 36.1–38.0 |
| ความดันตัวบน mmHg | ≤90, ≥220 | 91–100 | 101–110 | 111–219 |
| ชีพจร/นาที | ≤40, ≥131 | 111–130 | 41–50 / 91–110 | 51–90 |
| ระดับรู้สึกตัว (ACVPU) | สับสนใหม่/ซึม/ไม่ตอบสนอง | — | — | รู้สึกตัวดี |

**แถบเสี่ยงจากคะแนนรวม:**
- 0 = ปกติ (เขียว)
- 1–4 = เฝ้าดู (เหลือง)
- 5–6 **หรือ** พารามิเตอร์เดี่ยวใดได้ 3 = ควรหาหมอ (แดง)
- ≥7 = ด่วนมาก (แดงเข้ม)

**กติกาสำคัญ:** "พารามิเตอร์เดี่ยว = 3 → แดงทันที" คือ safety net —
ค่าเดียวผิดปกติมากก็ escalate แม้คะแนนรวมต่ำ (รองรับ "ผู้สูงอายุอาการไม่ตรงไปตรงมา").

## mapping ที่จะเปลี่ยน (แทนของเดิม)
แทน "LLM 0–10 → 8–10 = แดง" ด้วย:
- **แดง** = NEWS2 รวม ≥5 **หรือ** พารามิเตอร์เดี่ยว = 3 **หรือ** symptom red-flag rule ยิง
- **เหลือง** = NEWS2 1–4 หรือมี watch-modifier
- **เขียว** = 0 / อาการธรรมดาไม่มี red-flag

ตัวอย่าง: "ปวดท้อง" vital ปกติ = เขียว/เหลือง · "ปวดท้อง + ไข้ 39 + ชีพจร 120" = NEWS2 สูง = แดงอัตโนมัติ.

## Symptom red-flag rules (track 2)
อาการธรรมดา (ปวดท้อง, ปวดหัว, ไอ...) = ไม่แดง เว้นแต่มี modifier.

> ⚠️ **สถานะที่มา (สำคัญ — ยังไม่ควร ship จนกว่าจะ ground ครบ):**
> รายการด้านล่างยัง **ไม่ใช่ rule set ที่ validated ต่อชิ้น** — เป็นโครง/ตัวอย่าง. แยกที่มาชัดๆ:

**✅ มีแหล่งอ้างอิง (จาก search รอบเบา):**
- ความดันตัวบน < 90, หมดสติ/loss of consciousness — *ML triage paper* (nature s41598-021-03104-2)
- เจ็บหน้าอก + เหงื่อแตก (cold sweat); ปวดหัว + ระดับรู้สึกตัวเปลี่ยน — *online symptom-checker red-flags* (PMC12486864)

**✍️ ผมแต่งเป็นตัวอย่างเอง (ความรู้คลินิกทั่วไป — ยังไม่มี citation ต่อชิ้น, ต้องหาแหล่งก่อนใช้):**
- ของเหลว/เลือดผิดปกติ: อาเจียนเป็นเลือด, ถ่ายดำ/เป็นเลือด
- ความรุนแรง/ระยะเวลา: ปวดรุนแรง/ปวดต่อเนื่องหลายวัน, ท้องแข็ง
- บริบท: ผู้สูงอายุ + เฉียบพลัน, ไข้สูงร่วม

**TODO:** แทนรายการ "✍️ แต่งเอง" ด้วยตาราง red-flag ที่มี citation ต่ออาการ — รอผล **deep-research รอบใหญ่
(คำถามข้อ 3: red-flag frameworks)** ที่กำลังรวบรวมไกด์ไลน์อยู่. แหล่งที่ตั้งใจใช้ ground: NICE red-flag ต่ออาการ,
ตำรา clinical red flags, แนวทางไทย (สปสช./ราชวิทยาลัย).

รูปแบบที่จะเก็บ: ตาราง `{ อาการฐาน → red-flag keywords + แหล่งอ้างอิง }` ให้ code เช็ค (LLM สกัดคำให้ ไม่ตัดสิน).

## ปัจจัยเสริม (เฟสหลัง ถ้าจะละเอียดขึ้น)
- **Charlson Comorbidity Index** — ถ่วงน้ำหนักโรคประจำตัว + อายุ (+1 ต่อทุก 10 ปี หลัง 50) เป็น modifier
  ยกความเสี่ยงพื้นฐาน (แอปมี `diseases` + `age` แล้ว)
- NEWS2 มีงานยืนยันใช้กับผู้สูงอายุเปราะบางได้ (PMC7594283)

## ข้อควรระวัง (medical/liability)
- NEWS2 ออกแบบสำหรับการเสื่อมเฉียบพลัน "ในที่ที่มี monitor" — เอามาใช้กับค่าที่ญาติวัดเองที่บ้าน
  เป็นการดัดแปลง: **คงคำเตือน "ปรึกษาแพทย์" เสมอ** และ bias ไปทางระวังไว้ก่อน
- **ค่าที่ขาด (ไม่ได้วัด) อย่าตีความว่าปกติ** — คำนวณเท่าที่มี + บอกชัดว่าข้อมูลไม่ครบ ไม่ใช่ "ปลอดภัย"
- คะแนน = "ความเร่งด่วน/ความควรใส่ใจ" ไม่ใช่ชื่อโรค — ห้ามวินิจฉัย

## แผน implement (ยังไม่ทำ)
- `lib/risk.ts`: `news2Score(vitals) → { total, perParam, band, incomplete }` (deterministic, มี test)
- red-flag rule table + ฟังก์ชันเช็ค
- รวม band สุดท้าย = max(NEWS2 band, red-flag band, ...) → ป้อน doctor-score / สีในปฏิทิน/ไทม์ไลน์
- เพิ่มช่องกรอก resp rate + ระดับรู้สึกตัว (optional) เพื่อให้ NEWS2 ครบ

## Sources
- NEWS2 — Royal College of Physicians: https://www.rcp.ac.uk/resources/national-early-warning-score-news-2/
- NEWS2 scoring & escalation — iatrox: https://www.iatrox.com/blog/news2-scoring-escalation-guide
- NEWS calculator — MetricGate: https://metricgate.com/docs/news-early-warning/
- NEWS2 & triage in frail older adults — PMC7594283: https://www.ncbi.nlm.nih.gov/pmc/articles/PMC7594283/
- Red flags in online symptom checkers — PMC12486864: https://pmc.ncbi.nlm.nih.gov/articles/PMC12486864/

> หมายเหตุ: ยังมี deep-research รอบใหญ่ (triage systems ESI/Manchester/CTAS, teletriage, red-flag frameworks)
> รันค้างไว้ — เอาผลมาเสริมเอกสารนี้ได้ภายหลัง.
